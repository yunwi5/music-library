"""App entry point."""
from music import create_app

app = create_app()

# if __name__ == "__main__":
    # port = environ.get('PORT')
    # print(f"port: {port}")
    # app.run(host='0.0.0.0', port=5000)
